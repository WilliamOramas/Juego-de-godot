# Godot-specific ignores
.godot/
*.translation

# Mono-specific ignores (for C# if used in future)
.mono/
data_*/
mono_crash.*.json

# System/Tool ignores
*.tmp
*.log
~*

# Export ignores (build builds)
/build/
/exports/
export_presets.cfg

# Local configs
supabase.cfg
ai.cfg
config.cfg

# Node (Supabase migration scripts in tools/)
node_modules/
tools/node_modules/
